export default function HomeNoCity() {
    return (
        <>
            <h1>Add your city to get your frost date, then start adding your seed packets!</h1>
        </>
    )
}

//{card.map((card) => (<SeedPacket key={card.id} setCards={setCards} />))}